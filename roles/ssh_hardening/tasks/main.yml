---
- name: Deploy hardened sshd_config
  template:
    src: sshd_config.j2
    dest: /etc/ssh/sshd_config.d/hardened.conf
    owner: root
    group: root
    mode: "0600"
    validate: /usr/bin/sshd -t -f %s
  notify: Restart sshd
